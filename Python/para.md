
```
import argparse
parser = argparse.ArgumentParser(description='Extract ployA region based on old borders.')

from argparse import ArgumentParser
parser = ArgumentParser(description='Segmentation raw signals.')
parser.add_argument('--type', type=int, default=10)
```
